
<div data-test="foo">
  <div class="alert-banner-icon">
    <Icon
      @size="l"
      aria-hidden=true
      @glyph={{messageinfo.glyph}}
    />
  </div>
  <div class="alert-banner-message">
    <div class="alert-banner-message-title">
      {{#if @title}}
        {{@title}}
      {{else}}
        {{@type}}
      {{/if}}
    </div>
    {{#if @message}}
      <p class="alert-banner-message-body">
        {{@message}}
      </p>
    {{/if}}
    {{#if (has-block)}}
      <p class="alert-banner-message-actions">
        {{yield}}
      </p>
    {{/if}}
  </div>
</div>
